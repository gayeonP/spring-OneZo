spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    show-sql: true

logging.level:
  org.hibernate.orm.jdbc.bind: trace
  org.springframework.cache: TRACE
